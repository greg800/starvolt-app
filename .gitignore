node_modules
dist
.env
.env.local
.DS_Store

# Config locale Claude Code : contient des secrets (PAT Supabase, clés API)
# dans l'allowlist des permissions → ne JAMAIS committer.
.claude/settings.local.json

# PAT Supabase (jamais committé)
.claude/.supabase-pat

# tout fichier contenant le token Supabase, quel que soit son nom
*supabase-pat*

# état local de la CLI Supabase (version téléchargée, projet lié)
supabase/.temp/
